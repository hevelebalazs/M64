struct Bitmap
{
	width:Int32;
	height:Int32;
	memory:@UInt32;
}

func ClearBitmap(bitmap:@Bitmap, color_code:UInt32)
{
	for index := 0 to < bitmap.width * bitmap.height
	{
		bitmap.memory[index] = color_code;
	}
}