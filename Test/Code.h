typedef struct Bitmap
{
    unsigned int * memory;
    int width;
    int height;
} Bitmap;

void UpdateBitmap( *bitmap)
{
    unsigned int *pixel = ;
    for(int row = 0; row < ; row++)
    {
        for(int col = 0; col < ; col++)
        {
             = ;
            pixel++;
        }

    }

}
